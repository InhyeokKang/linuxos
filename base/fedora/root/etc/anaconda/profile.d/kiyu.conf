# kiyu 설치 프로그램(Anaconda) 프로필. os-release 의 ID=kiyu 로 선택되며 Fedora 프로필을 상속한다.
[Profile]
profile_id = kiyu
base_profile = fedora

[Profile Detection]
os_id = kiyu

[Localization]
use_geolocation = False

[Storage]
default_scheme = BTRFS
btrfs_compression = zstd:1
